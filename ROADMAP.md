# خطة برق (Barq) — المراحل والحالة

## Phase 1 — الأساس اليدوي (صفر ذكاء اصطناعي)
الهدف: أدمن واحد يقدر يعمل قالب بخاناته يدوياً، يعمل منه مشروع، يملّي الخانات، والموقع
يتولّد على طول على سب دومين خاص بيه.

| # | المهمة | الحالة |
|---|--------|--------|
| 1 | تأسيس مشروع Laravel 13 باسم "barq" | ✅ خلصت |
| 2 | نظام دخول بسيط بمستخدم واحد (أدمن) | ✅ خلصت |
| 3 | الـ migrations والموديلات (templates, template_variants, template_slots, projects, generated_sites, unsupported_requests) | ✅ خلصت |
| 4 | واجهة الأدمن: إضافة قالب يدوي + تحديد خاناته | ✅ خلصت |
| 5 | صفحة "مشروع جديد" (حقول يدوية، صفر ذكاء اصطناعي لسه) | ✅ خلصت |
| 6 | رندر الموقع على السب دومين (`DetectSite` middleware) | ✅ خلصت |
| 7 | توثيق المشروع (`CLAUDE.md` + `ROADMAP.md`) | ✅ خلصت |
| 8 | `git init` + أول commit + رفعه على GitHub | 🟡 `git init` وأول commit اتعملوا محلي — الرفع مستني ريبو حقيقي (تفاصيل تحت) |

### تفاصيل ما اتعمل (Tasks 1-6)
- **الموديلات:** `Template` (بفئة/نوع `landing`|`wordpress`/ملاحظة ترخيص) له `variants` (كل
  variant بألوانه وترتيب أقسامه) و`slots` (كل خانة باسمها العربي ونوعها وترتيبها). `Project`
  بياخد قالب + variant اختياري، وله `site` واحد (`GeneratedSite`) بمحتواه (`content_json`)
  وحالته (`draft`/`published`/`archived`).
- **لوحة التحكم:** CRUD كامل للقوالب وخاناتها و variants بتاعتها، وCRUD للمشاريع مع صفحة
  تعديل محتوى الموقع (`site-edit.blade.php`) بتعرض كل خانة حسب نوعها.
- **الرندر العام:** `routes/site.php` مسجّل بس تحت `{siteSlug}.barq.tafraos.com` (شوف تفصيل
  المعمار في `CLAUDE.md`). موقع بلا محتوى بيورّي "لسه بيتجهّز"، قالب `wordpress` بيورّي نفس
  الرسالة (البنية التحتية بتاعته مؤجّلة لـ Phase 5).
- **الاختبارات:** 4 ملفات (`AuthenticationTest`, `TemplateManagementTest`,
  `ProjectManagementTest`, `SiteRenderingTest`) — 49 تست، كلها شغّالة.
- **الأمان:** حساب أدمن واحد بس عن طريق `php artisan barq:create-admin` (صفر تسجيل حسابات،
  صفر استرجاع باسورد) — تفاصيله في `CLAUDE.md`.

### المتبقي (Task 8)
`git init` وأول commit اتعملوا محلي بالفعل (`67d9df3` — كل شغل Phase 1). المتبقي بس هو
الرفع على GitHub، وده اتشخّص بدقة: ريبو باسم `barq` تحت حساب `fmffoash` **مش موجود على
GitHub خالص** — اتأكد بـ 3 فحوصات حقيقية مستقلة (بحث الريبوهات رجّع صفر نتيجة، أداة إنشاء
ريبو جديد رجّعت `403: Resource not accessible by integration` مرتين منفصلتين، وأداة ربط
ريبو موجود رجّعت "مش موجود أو الصلاحية الحالية معهاش وصول ليه"). صلاحية هذا الـ session
الحالية على GitHub مقصورة على الريبوهات اللي اتديت وصول ليها بالفعل (`tafra-erp` بس)، وصفر
أداة متاحة تقدر تنشئ ريبو جديد بنفسها.

**الحل الوحيد المتاح — يحتاج فعل من المستخدم:**
1. ينشئ ريبو فاضي باسم `barq` تحت حساب `fmffoash` على GitHub.com — **بدون** تفعيل
   README/gitignore/license (عشان الريبو الفاضي يستقبل الـ commit المحلي الموجود من غير
   تعارض).
2. لو لسه مش متاح للـ session بعد الإنشاء، يدّي الـ GitHub App المثبّتة صلاحية وصول صريحة
   للريبو الجديد ده (نفس الطريقة اللي اتديت بيها صلاحية `tafra-erp`).
بمجرد ما ده يتظبط، الرفع هيتم فوراً — كل شغل Phase 1 و Phase 2 جاهز ومحفوظ محلياً.

---

## Phase 2 — الذكاء الاصطناعي (Ollama) — ✅ خلصت
الهدف: بدل ما الأدمن يملّي كل خانة يدوياً، يقدر يوصف الموقع اللي عايزه بجملة أو اتنين
(زي "مطعم فطاير في المهندسين") والنظام يقترح محتوى مبدئي لكل خانة تلقائي — الأدمن بعدين
بيراجع ويعدّل.
- ✅ **`OllamaService`** (`app/Services/OllamaService.php`) — بيكلّم Ollama المحلي عن طريق
  `/api/generate` (`config('services.ollama.*')`، بيتظبط من `.env`: `OLLAMA_BASE_URL`/
  `OLLAMA_MODEL`). صفر بيانات بتتبعت لأي API خارجي — كله محلي على السيرفر نفسه.
- ✅ الـ prompt بيتبني من خانات القالب نفسه (`TemplateSlot`، مجمّعة حسب القسم)، بيحدد نوع كل
  خانة (نص/فقرة/قايمة/رابط) وبيطلب رد بصيغة JSON بس. الخانات من نوع صورة **مستبعدة دايماً** —
  الذكاء الاصطناعي مبيقترحش صور.
- ✅ فشل أي حاجة (Ollama مش متاح، رد مش JSON صالح، حالة HTTP غير ناجحة، مفتاح مش موجود في
  القالب) بيرجّع مصفوفة فاضية بهدوء — صفر كراش، الأدمن دايماً يقدر يكمّل يدوي.
- ✅ زرار "اقترح محتوى" في صفحة تعديل المشروع (`GET /projects/{project}/site`) — الأدمن
  بيكتب وصف قصير للنشاط، بيتبعت لـ `POST /projects/{project}/site/suggest`
  (`GeneratedSiteController::suggest`)، وبيعبّي **الخانات الفاضية بس** — أي خانة فيها محتوى
  مكتوب يدوي بالفعل بتفضل زي ما هي تماماً.
- ✅ 9 اختبارات مخصّصة (`OllamaContentSuggestionTest` + 3 اختبارات إضافية في
  `ProjectManagementTest`) بتغطي: الاقتراح الناجح + استبعاد الصور + استبعاد أي مفتاح مش
  موجود في القالب + عدم الدعس على محتوى مكتوب يدوي + الفشل الآمن (Ollama غير متاح/رد غير
  صالح/حالة HTTP فاشلة) + التحقق من إجبارية وصف النشاط. السويت الكامل 58 تست، كلهم شغّالين.

## Phase 3 — حفظ كقالب + بحث في مكتبة القوالب — ✅ خلصت
الهدف: مكتبة القوالب تكبر بمرور الوقت من غير ما الأدمن يبدأ من الصفر كل مرة، وسهولة إن
يلاقي القالب المناسب وسط مكتبة كبيرة قبل ما يعمل مشروع جديد.
- ✅ **"احفظ كقالب جديد"** (`TemplateController::storeFromProject`، زرار في صفحة المشروع
  `GET /projects/{project}`) — بيولّد قالب مستقل تماماً (خانات منسوخة، نسخة variant منسوخة
  لو المشروع مستخدم واحدة) من غير ما يأثّر على القالب الأصلي. خياران: "بمحتواه" (قيم الموقع
  الحالي بتبقى القيمة الافتراضية لكل خانة) أو من غيره (بيورّث الافتراضي القديم لو موجود).
  التصنيف بيتوّرث من القالب الأصلي لو الأدمن سابه فاضي.
- ✅ **تعبئة مبدئية للمشروع الجديد** (`ProjectController::defaultContentFromTemplate`) —
  أي خانة عندها `TemplateSlot.default_value` (عمود `json` جديد على `template_slots`) بتتحط
  تلقائي كمحتوى ابتدائي للموقع الناتج، بدل ما يبدأ فاضي تماماً.
- ✅ **بحث وفلترة مكتبة القوالب** — فورم GET (بحث بالاسم `q` + دروب داون فئة `category` مبني
  ديناميكي من `Template::distinct()->pluck('category')`) في صفحة `templates.index`
  **وفي** خطوة اختيار القالب وقت عمل مشروع جديد (`projects.create`) — نفس الآلية في المكانين.
- ✅ 10 اختبارات جديدة (`TemplateLibraryTest`) بتغطي: الحفظ كقالب بمحتوى/من غيره، إجبارية
  الاسم، توريث التصنيف لما يتسيب فاضي، نسخ الـ variant الافتراضية، تعبئة المشروع الجديد من
  الافتراضي (وحالة عدم وجود افتراضي)، والبحث/الفلترة في الشاشتين. السويت الكامل 68 تست،
  كلهم شغّالين.

## Phase 4 — تصدير كملفات ثابتة — ✅ خلصت
الهدف: العميل يقدر ياخد نسخة HTML/CSS ثابتة بالكامل من موقعه (بدون أي اعتماد على Laravel
وقت التشغيل) لو عايز يستضيفها بنفسه بره برق.
- ✅ **`SiteRenderer`** (`app/Services/SiteRenderer.php`) — استخرجنا منطق بناء الأقسام
  المرتّبة/المفلترة والألوان النهائية من `SiteController` لخدمة مشتركة، عشان نفس المنطق
  يُستخدم في المعاينة الحية والتصدير الثابت من غير تكرار.
- ✅ **`SiteExportService`** (`app/Services/SiteExportService.php`) — بيبني حزمة تصدير كاملة:
  بيرندر `index.html` (فيو `site.export` — نسخة من `site.show` بس بلينك CSS نسبي بدل
  `@vite`)، بينسخ CSS المولّد بالـ build + كل خطوط Cairo اللي بيعتمد عليها (القايمة جاية من
  `manifest.json` بتاع Vite نفسه، صفر تخمين)، وبيعيد كتابة مسارات الخطوط المطلقة (`/build/
  assets/...`) لمسارات نسبية عشان الحزمة تفضل شغّالة من أي مكان. أي خانة صورة بمسار
  `/storage/{path}` بتتحوّل لمسار نسبي `assets/images/{filename}` جوّه الحزمة، والملف الحقيقي
  بينسخ معاها (عن طريق `Storage::disk('public')`، مش مسار مباشر مهاردكودد — عشان يشتغل صح
  حتى مع أقراص تخزين وهمية في التست). كل حاجة بتتحط جوّه مجلد مؤقت وتتضغط zip واحد
  (`ZipArchive`)، والمجلد المؤقت بيتمسح بعد كده. متاح بس للقوالب من نوع "landing" — قوالب
  "wordpress" مالهاش رندر حقيقي أصلاً لسه (Phase 5).
- ✅ زرار "صدّر الموقع" في صفحة المشروع (`GET /projects/{project}/site/export`،
  `GeneratedSiteController::export`) — بيظهر بس لما القالب `landing`، وبيولّد ملف zip
  ويحمّله على طول. لو القالب `wordpress`، بيرجّع رسالة واضحة بدل ما يحاول يصدّر صفحة "قريباً"
  بلا فايدة. عمود `GeneratedSite.exported_at` بيتحدّث بعد كل تصدير ناجح، ومعروض في صفحة
  المشروع جنب "آخر تحديث للمحتوى".
- ✅ 4 اختبارات جديدة (`SiteExportTest`) بتغطي: ظهور/اختفاء زرار التصدير حسب نوع القالب،
  تصدير ناجح لموقع حقيقي (الـ zip فيه `index.html` بالمحتوى الصح و`assets/app.css`)، إعادة
  كتابة مسار صورة مرفوعة وتضمين الملف الحقيقي جوّه الحزمة، ورفض تصدير قالب `wordpress`
  برسالة واضحة من غير ما يحدّث `exported_at`. السويت الكامل 72 تست، كلهم شغّالين.

## Phase 5 — WordPress Multisite — ✅ خلصت
الهدف: قوالب من نوع "ووردبريس" تبقى شغّالة فعلياً — مشروع جديد يقدر يعمل site حقيقي على
شبكة WordPress Multisite منفصلة، ويتبعتله المحتوى، من غير أي تدخّل يدوي على الشبكة نفسها.
- ✅ **`WordPressService`** (`app/Services/WordPressService.php`) — نفس فلسفة `OllamaService`
  بالحرف: كل نداء HTTP ملفوف بـ `try/catch`، وأي فشل (الشبكة مش متاحة، رد مش ناجح، شكل الرد
  مش متوقّع، أو الإعدادات ناقصة) بيتسجّل في اللوج ويرجّع `false` من غير ما يكسر الطلب — الأدمن
  يقدر يحاول تاني من نفس الزرار وقت ما يحب. `provisionSite()` **idempotent** فعلياً: لو الموقع
  اتعمل بالفعل (`wp_site_id` مش فاضي) بيرجع `true` على طول من غير نداء تاني للشبكة. الاستثناء
  الوحيد اللي بيترمي فيه هو استدعاء الدالة لمشروع مش من نوع "ووردبريس" (`RuntimeException`،
  نفس نمط `SiteExportService::export()` بالظبط).
- ✅ **`docs/wordpress-mu-plugin.php`** — ملف deliverable حقيقي (مش كود لارافيل، بينتقل يدوي
  لـ `wp-content/mu-plugins/` على شبكة الـ WordPress نفسها) بيعرّض REST namespace اسمه
  `barq/v1`: `POST /sites` (بتعمل site كامل فعلياً عن طريق `wpmu_create_blog()` — نفس الدالة
  اللي شاشة "Add New Site" في Network Admin بتستخدمها، مش مجرد صف في الداتابيز — والدومين/
  المسار بيتحدد تلقائي حسب نوع تنصيب الشبكة نفسها subdomain/subdirectory عن طريق
  `is_subdomain_install()`، برق مش مفروض يعرف أو يخمّن نظام دومين الشبكة) و`POST /sites/{id}/
  content` (بتفسّر مفاتيح معروفة بس: `site_title`/`site_tagline`/`homepage_body`، وتتجاهل أي
  مفتاح تاني بأمان). محمي بتوكن ثابت (Bearer، مقارنة بـ `hash_equals`) ضد قيمة معرّفة في
  `wp-config.php` بتاع الشبكة — صفر باسورد أو سر حقيقي متسجّل في أي ملف كود.
- ✅ **الربط في الكونترولر:** `GeneratedSiteController::provisionWordPress()` (زرار "اعمل site
  على ووردبريس" في صفحة المشروع، بيظهر بس للمشاريع من نوع wordpress اللي لسه ما اتعملتلهاش
  site) و`pushWordPressContent()` (زرار "حدّث المحتوى على ووردبريس" + لينك "لوحة تحكم ووردبريس"،
  بيظهروا بعد ما الموقع يتعمل). **صفر فورم منفصل مخصّص لووردبريس** — المحتوى اللي بيتبعت هو
  نفسه `content_json` المتملي من فورم "تعبئة المحتوى" العادي (يدوي أو باقتراح الذكاء الاصطناعي
  من Phase 2 — الدالتين دول أصلاً مش بيفرّقوا في نوع القالب، فبيشتغلوا مع مشاريع الووردبريس
  زي ما هم بالظبط من غير أي تعديل).
- ✅ **`SiteController::show()`** (عرض الموقع للزائر الحقيقي على السب دومين) بقى بيتحقق من
  `GeneratedSite::isWordPressProvisioned()` — لو الموقع اتعمل فعلاً على الشبكة، الزائر بيتحوّل
  (`redirect()->away(...)`) لموقعه الحقيقي هناك على طول؛ لو لسه لأ، بيفضل يعرض صفحة "قريباً"
  زي الأول. نفس المنطق مطبّق في `GeneratedSite::previewUrl()` (اللينك المعروض في صفحة المشروع
  نفسها).
- ✅ 16 اختبار جديد (`WordPressIntegrationTest` + تست إضافي في `SiteRenderingTest`) بتغطي:
  رفض نوع قالب غلط، idempotency التوفير، فشل هادئ لما الإعدادات ناقصة/الشبكة مش متاحة/الرد
  مش متوقّع، نجاح التوفير وتخزين بيانات الموقع الراجعة، نفس الحالات لدفع المحتوى، ظهور/اختفاء
  الأزرار حسب حالة الموقع، وسلوك الكونترولر والـ redirect العام. السويت الكامل 88 تست، كلهم
  شغّالين.

## Phase 6 — تجهيز النشر على السيرفر (Deployment Readiness) — ✅ توثيق/تجهيز خلص، تنفيذ فعلي لسه
الهدف: كل الملفات والخطوات اللي محتاجة تحصل عشان برق يبقى شغّال حي على `barq.tafraos.com`
على نفس سيرفر Hetzner اللي شغّال عليه Tafra ERP — بس مستقل بالكامل (كود/داتابيز/كونفيج
منفصلين تماماً). **⚠️ ملحوظة أمانة مهمة:** الجلسة اللي عملت الفيز دي مالهاش أي وصول SSH فعلي
للسيرفر (صفر `ssh`/`scp` binary، صفر مفتاح جوّه `~/.ssh/`) — يعني كل اللي اتعمل هنا تجهيز/
توثيق للخطوات المطلوبة، مش تنفيذ فعلي حصل على السيرفر. التنفيذ الحقيقي محتاج شخص أو جلسة
تانية عندها وصول SSH حقيقي لـ `138.199.220.217`.
- ✅ **`deploy/nginx-barq.conf`** — كونفيج nginx كامل لـ `barq.tafraos.com` + `*.barq.tafraos.com`
  مع بعض في نفس الـ server block (الدومين الواحد بيغطي لوحة التحكم والمواقع المنشورة، والفرق
  بينهم بيتحسم جوّه Laravel نفسه بالـ domain routing الموجود أصلاً — مش هنا في nginx). نفس
  دفاع الأمان الموجود في كونفيج الـ ERP (`location ~* ^/storage/.*\.php$ { deny all; }`) ضد
  تنفيذ PHP جوّه مجلد رفع الصور العام. شهادة SSL من نوع Cloudflare Origin CA (زي الـ ERP بالظبط،
  مش Let's Encrypt) عشان يدعم wildcard (`*.barq.tafraos.com`) من غير تعقيد DNS-01 challenge.
  مسارات وملفات مستقلة بالكامل عن كونفيج الـ ERP (`/var/www/barq`، `/etc/ssl/barq/`،
  `sites-enabled/barq`).
- ✅ **`deploy/env.production.example`** — قالب `.env` كامل للإنتاج، **صفر سر أو باسورد حقيقي
  فيه** (كل قيمة حساسة `__PLACEHOLDER__` لازم تتكتب بإيد الشخص اللي بينفّذ مباشرة على السيرفر).
  `DB_CONNECTION=mysql` (بدل SQLite المحلي) بقاعدة بيانات ويوزر مستقلين تماماً عن قاعدة
  بيانات الـ ERP (`barq` مش `erp`). كل قسم اختياري (Ollama، شبكة WordPress Multisite) معلّق
  عليه بوضوح إنه آمن يتسيب فاضي لحد ما يتحتاج فعلاً — الميزات المرتبطة بيهم بترجع تفشل بأمان
  من غير ما تكسر باقي النظام (`OllamaService`/`WordPressService` الاتنين مبنيين على نفس
  فلسفة `try/catch` الآمنة من زمان الفيزات اللي فاتت).
- ✅ **`deploy/DEPLOYMENT.md`** — دليل خطوة بخطوة كامل (15 قسم): متطلبات السيرفر، سجلات DNS
  على Cloudflare، إصدار شهادة Cloudflare Origin CA، إنشاء قاعدة بيانات MySQL مستقلة، جلب
  الكود وبناء الأصول، إعداد `.env`، الـ migrations + `storage:link` (ضروري فعلاً — `GeneratedSiteController`
  بيخزّن صور المواقع على `disk('public')`، ولازم اللينك الرمزي ده عشان تظهر للزوار)، إنشاء
  حساب الأدمن، الكاش والصلاحيات، تفعيل nginx، وملحوظتين مهمّتين: **مفيش أي scheduled command
  ولا queued job موجودين فعلياً في الكود لحد دلوقتي** (اتأكد بالفحص المباشر — `routes/console.php`
  فيها بس الأمر الافتراضي، وصفر `ShouldQueue`/`dispatch(` في المشروع كله) فمفيش داعي لـ cron
  أو queue worker systemd service دلوقتي — موثّق صراحة عشان محدش يضيّع وقت في بنية تحتية مش
  محتاجة. + اختبار دخان (smoke test) نهائي بعد كل الخطوات.
- ✅ **تصحيح دقة في `CLAUDE.md`:** القيد الحقيقي لإصدار PHP هو `^8.3` (من `composer.json`
  مباشرة)، مش "PHP 8.5" زي ما كان مكتوب غلط — الرقم الغلط ده جاي من الـ boilerplate العام
  بتاع Laravel Boost في `AGENTS.md`، مش من قيد المشروع الحقيقي. + توثيق مجلد `deploy/` الجديد
  في هيكل المجلدات، مع نفس التنبيه الصريح إن ده تجهيز/توثيق مش تنفيذ فعلي.

**الخلاصة:** كل حاجة ممكن تتعمل من جانب الكود/الريبو خلصت. النشر الفعلي (تشغيل الأوامر دي
حرفياً على `138.199.220.217`) محتاج جلسة أو شخص عنده وصول SSH حقيقي — مش شيء ممكن يحصل من
هنا.

---

## قواعد ثابتة (مرجع سريع — التفاصيل الكاملة في `CLAUDE.md`)
- الـ working directory بيرجع لـ `/home/user/tafra-erp` بعد كل أمر Bash — استخدم
  `cd /home/user/barq &&` دايماً.
- اقرا بـ `Read` قبل أي `Edit`/`Write`.
- صفر باسورد حقيقي يتسجّل في أي ملف.
- كل تعديل: `php -l` + `php artisan test` قبل ما تعتبره خلص.
