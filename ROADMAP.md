# خطة برق (Barq) — المراحل والحالة

## Phase 1 — الأساس اليدوي (صفر ذكاء اصطناعي)
الهدف: أدمن واحد يقدر يعمل قالب بخاناته يدوياً، يعمل منه مشروع، يملّي الخانات، والموقع
يتولّد على طول على سب دومين خاص بيه.

| # | المهمة | الحالة |
|---|--------|--------|
| 1 | تأسيس مشروع Laravel 13 باسم "barq" | ✅ خلصت |
| 2 | نظام دخول بسيط بمستخدم واحد (أدمن) | ✅ خلصت |
| 3 | الـ migrations والموديلات (templates, template_variants, template_slots, projects, generated_sites, unsupported_requests) | ✅ خلصت |
| 4 | واجهة الأدمن: إضافة قالب يدوي + تحديد خاناته | ✅ خلصت |
| 5 | صفحة "مشروع جديد" (حقول يدوية، صفر ذكاء اصطناعي لسه) | ✅ خلصت |
| 6 | رندر الموقع على السب دومين (`DetectSite` middleware) | ✅ خلصت |
| 7 | توثيق المشروع (`CLAUDE.md` + `ROADMAP.md`) | ✅ خلصت |
| 8 | `git init` + أول commit + رفعه على GitHub | 🟡 `git init` وأول commit اتعملوا محلي — الرفع مستني ريبو حقيقي (تفاصيل تحت) |

### تفاصيل ما اتعمل (Tasks 1-6)
- **الموديلات:** `Template` (بفئة/نوع `landing`|`wordpress`/ملاحظة ترخيص) له `variants` (كل
  variant بألوانه وترتيب أقسامه) و`slots` (كل خانة باسمها العربي ونوعها وترتيبها). `Project`
  بياخد قالب + variant اختياري، وله `site` واحد (`GeneratedSite`) بمحتواه (`content_json`)
  وحالته (`draft`/`published`/`archived`).
- **لوحة التحكم:** CRUD كامل للقوالب وخاناتها و variants بتاعتها، وCRUD للمشاريع مع صفحة
  تعديل محتوى الموقع (`site-edit.blade.php`) بتعرض كل خانة حسب نوعها.
- **الرندر العام:** `routes/site.php` مسجّل بس تحت `{siteSlug}.barq.tafraos.com` (شوف تفصيل
  المعمار في `CLAUDE.md`). موقع بلا محتوى بيورّي "لسه بيتجهّز"، قالب `wordpress` بيورّي نفس
  الرسالة (البنية التحتية بتاعته مؤجّلة لـ Phase 5).
- **الاختبارات:** 4 ملفات (`AuthenticationTest`, `TemplateManagementTest`,
  `ProjectManagementTest`, `SiteRenderingTest`) — 49 تست، كلها شغّالة.
- **الأمان:** حساب أدمن واحد بس عن طريق `php artisan barq:create-admin` (صفر تسجيل حسابات،
  صفر استرجاع باسورد) — تفاصيله في `CLAUDE.md`.

### المتبقي (Task 8)
`git init` وأول commit اتعملوا محلي بالفعل (`67d9df3` — كل شغل Phase 1). المتبقي بس هو
الرفع على GitHub، وده اتشخّص بدقة: ريبو باسم `barq` تحت حساب `fmffoash` **مش موجود على
GitHub خالص** — اتأكد بـ 3 فحوصات حقيقية مستقلة (بحث الريبوهات رجّع صفر نتيجة، أداة إنشاء
ريبو جديد رجّعت `403: Resource not accessible by integration` مرتين منفصلتين، وأداة ربط
ريبو موجود رجّعت "مش موجود أو الصلاحية الحالية معهاش وصول ليه"). صلاحية هذا الـ session
الحالية على GitHub مقصورة على الريبوهات اللي اتديت وصول ليها بالفعل (`tafra-erp` بس)، وصفر
أداة متاحة تقدر تنشئ ريبو جديد بنفسها.

**الحل الوحيد المتاح — يحتاج فعل من المستخدم:**
1. ينشئ ريبو فاضي باسم `barq` تحت حساب `fmffoash` على GitHub.com — **بدون** تفعيل
   README/gitignore/license (عشان الريبو الفاضي يستقبل الـ commit المحلي الموجود من غير
   تعارض).
2. لو لسه مش متاح للـ session بعد الإنشاء، يدّي الـ GitHub App المثبّتة صلاحية وصول صريحة
   للريبو الجديد ده (نفس الطريقة اللي اتديت بيها صلاحية `tafra-erp`).
بمجرد ما ده يتظبط، الرفع هيتم فوراً — كل شغل Phase 1 و Phase 2 جاهز ومحفوظ محلياً.

---

## Phase 2 — الذكاء الاصطناعي (Ollama) — ✅ خلصت
الهدف: بدل ما الأدمن يملّي كل خانة يدوياً، يقدر يوصف الموقع اللي عايزه بجملة أو اتنين
(زي "مطعم فطاير في المهندسين") والنظام يقترح محتوى مبدئي لكل خانة تلقائي — الأدمن بعدين
بيراجع ويعدّل.
- ✅ **`OllamaService`** (`app/Services/OllamaService.php`) — بيكلّم Ollama المحلي عن طريق
  `/api/generate` (`config('services.ollama.*')`، بيتظبط من `.env`: `OLLAMA_BASE_URL`/
  `OLLAMA_MODEL`). صفر بيانات بتتبعت لأي API خارجي — كله محلي على السيرفر نفسه.
- ✅ الـ prompt بيتبني من خانات القالب نفسه (`TemplateSlot`، مجمّعة حسب القسم)، بيحدد نوع كل
  خانة (نص/فقرة/قايمة/رابط) وبيطلب رد بصيغة JSON بس. الخانات من نوع صورة **مستبعدة دايماً** —
  الذكاء الاصطناعي مبيقترحش صور.
- ✅ فشل أي حاجة (Ollama مش متاح، رد مش JSON صالح، حالة HTTP غير ناجحة، مفتاح مش موجود في
  القالب) بيرجّع مصفوفة فاضية بهدوء — صفر كراش، الأدمن دايماً يقدر يكمّل يدوي.
- ✅ زرار "اقترح محتوى" في صفحة تعديل المشروع (`GET /projects/{project}/site`) — الأدمن
  بيكتب وصف قصير للنشاط، بيتبعت لـ `POST /projects/{project}/site/suggest`
  (`GeneratedSiteController::suggest`)، وبيعبّي **الخانات الفاضية بس** — أي خانة فيها محتوى
  مكتوب يدوي بالفعل بتفضل زي ما هي تماماً.
- ✅ 9 اختبارات مخصّصة (`OllamaContentSuggestionTest` + 3 اختبارات إضافية في
  `ProjectManagementTest`) بتغطي: الاقتراح الناجح + استبعاد الصور + استبعاد أي مفتاح مش
  موجود في القالب + عدم الدعس على محتوى مكتوب يدوي + الفشل الآمن (Ollama غير متاح/رد غير
  صالح/حالة HTTP فاشلة) + التحقق من إجبارية وصف النشاط. السويت الكامل 58 تست، كلهم شغّالين.

## Phase 3 — حفظ كقالب + بحث في مكتبة القوالب — ✅ خلصت
الهدف: مكتبة القوالب تكبر بمرور الوقت من غير ما الأدمن يبدأ من الصفر كل مرة، وسهولة إن
يلاقي القالب المناسب وسط مكتبة كبيرة قبل ما يعمل مشروع جديد.
- ✅ **"احفظ كقالب جديد"** (`TemplateController::storeFromProject`، زرار في صفحة المشروع
  `GET /projects/{project}`) — بيولّد قالب مستقل تماماً (خانات منسوخة، نسخة variant منسوخة
  لو المشروع مستخدم واحدة) من غير ما يأثّر على القالب الأصلي. خياران: "بمحتواه" (قيم الموقع
  الحالي بتبقى القيمة الافتراضية لكل خانة) أو من غيره (بيورّث الافتراضي القديم لو موجود).
  التصنيف بيتوّرث من القالب الأصلي لو الأدمن سابه فاضي.
- ✅ **تعبئة مبدئية للمشروع الجديد** (`ProjectController::defaultContentFromTemplate`) —
  أي خانة عندها `TemplateSlot.default_value` (عمود `json` جديد على `template_slots`) بتتحط
  تلقائي كمحتوى ابتدائي للموقع الناتج، بدل ما يبدأ فاضي تماماً.
- ✅ **بحث وفلترة مكتبة القوالب** — فورم GET (بحث بالاسم `q` + دروب داون فئة `category` مبني
  ديناميكي من `Template::distinct()->pluck('category')`) في صفحة `templates.index`
  **وفي** خطوة اختيار القالب وقت عمل مشروع جديد (`projects.create`) — نفس الآلية في المكانين.
- ✅ 10 اختبارات جديدة (`TemplateLibraryTest`) بتغطي: الحفظ كقالب بمحتوى/من غيره، إجبارية
  الاسم، توريث التصنيف لما يتسيب فاضي، نسخ الـ variant الافتراضية، تعبئة المشروع الجديد من
  الافتراضي (وحالة عدم وجود افتراضي)، والبحث/الفلترة في الشاشتين. السويت الكامل 68 تست،
  كلهم شغّالين.

## Phase 4 — تصدير كملفات ثابتة
- زرار "صدّر الموقع" بيولّد نسخة HTML/CSS ثابتة بالكامل من المشروع (بدون أي اعتماد على
  Laravel وقت التشغيل) — مفيدة لو العميل عايز يستضيف الموقع بنفسه بره برق.
- تحدي رئيسي: تحويل رندر Blade الديناميكي (الألوان كـ CSS custom properties، الأقسام
  المفلترة) لملفات ثابتة صحيحة 100%.

## Phase 5 — WordPress Multisite + تعبئة تلقائية بالذكاء الاصطناعي
- تفعيل `Template.kind = 'wordpress'` فعلياً (دلوقتي بيورّي "لسه بيتجهّز" بس).
- تكامل مع WordPress Multisite: مشروع جديد بيعمل site جديد على الشبكة تلقائي.
- تعبئة محتوى الـ WordPress site بالذكاء الاصطناعي (امتداد لـ Phase 2، بس على منصة تانية).

---

## قواعد ثابتة (مرجع سريع — التفاصيل الكاملة في `CLAUDE.md`)
- الـ working directory بيرجع لـ `/home/user/tafra-erp` بعد كل أمر Bash — استخدم
  `cd /home/user/barq &&` دايماً.
- اقرا بـ `Read` قبل أي `Edit`/`Write`.
- صفر باسورد حقيقي يتسجّل في أي ملف.
- كل تعديل: `php -l` + `php artisan test` قبل ما تعتبره خلص.
